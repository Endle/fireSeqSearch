use log::info;
use crate::Article;

pub fn generate_wordlist(articles: &Vec<Article>) -> String {
    info!("Generating wordlist");

    "stub_cloud".to_string()
}

fn article_to_tokens(art: &article) -> Vec<String> {

}